#System bar-card for cpu, ram usage

system_bar_card:
  card:
    type: custom:bar-card
    entity: '[[entity]]'
    position:
      indicator: inside
      name: outside
      width:
    style: |
      bar-card-currentbar {
        border-radius: 5px;
        background-color: dwains-theme-background;
      } 
      bar-card-backgroundbar {
        border-radius: 5px;
        background-color: dwains-theme-background;
      }       